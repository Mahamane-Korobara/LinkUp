# Journal — Semaine 0 (Préparation)

## ✅ Fait

- Monorepo initialisé : `agent/`, `bridge/`, `mobile/`, `dashboard/`, `docs/`, `infra/`, `.github/`
- Git initialisé sur branche `main`, premier commit avec CDC + plan d'exécution
- `.gitignore` couvrant les 4 stacks + secrets + cache + builds
- **Agent Laravel 12** scaffolded — `composer create-project` → Pest installé → 2 tests verts
- **Bridge FastAPI** scaffolded — venv + pyproject.toml + endpoint `/health` + auth Bearer token interne → 4 tests pytest verts, ruff + black clean
- **Mobile Flutter 3.41** scaffolded — `flutter create --org tech.sahelstack.linkup --platforms=android` → analyze OK, test OK
- **Dashboard Next.js 15** scaffolded — `pnpm create next-app` (App Router + TS + Tailwind + Turbopack) → lint OK, build OK
- **CI GitHub Actions** : 4 workflows (`agent.yml`, `bridge.yml`, `mobile.yml`, `dashboard.yml`) + Dependabot weekly
- **README** monorepo + **ADR-001** rédigés

## 🤔 Décisions

- **pnpm 9** au lieu de pnpm 11 (bug ESM avec Node 20.20)
- **venv + pyproject.toml** au lieu de Poetry (évite dépendance externe ; pyproject.toml lisible directement par pip)
- **Pest 3.8** (compatible Laravel 12)
- **Flutter 3.41.9** (version installée localement, à figer dans CI)

## ⚠️ À surveiller

- Composer affiche un warning de dépréciation `E_STRICT` — sans impact sur l'install, à ignorer
- `corepack` Node 20.20 a un bug avec pnpm 11 → utiliser `npm install -g pnpm@9` direct
- L'utilisateur n'a pas encore poussé le repo sur GitHub — CI non encore validées en runner réel

## 🚦 Gate W0

- [x] Scaffolds des 4 sous-projets compilent et passent leurs tests
- [x] Workflows CI rédigés
- [x] README + ADR-001 présents
- [x] Premier commit sur `main`

**Verdict :** W0 bouclée. **Prochain bloc : S1 — Reverb + FastAPI live + mDNS.**

## ➡️ Prochaines étapes

1. Pousser le repo sur GitHub (`git remote add origin … && git push -u origin main`)
2. Démarrer S1.J1 : installer `laravel/reverb` et créer `PingEvent`
