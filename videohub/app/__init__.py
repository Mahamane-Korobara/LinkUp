"""Linkup VideoHub — service FastAPI internet-facing (VPS).

Outil STANDALONE (sans PC appairé) : résout un lien vidéo de n'importe quelle
plateforme (via yt-dlp), le télécharge / extrait l'audio, et renvoie un transcript
formaté à partir des sous-titres. Distinct du bridge PC (LAN, bundlé).
"""

__version__ = "0.1.0"
